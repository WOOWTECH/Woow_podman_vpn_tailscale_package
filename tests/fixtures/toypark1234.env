# toypark1234 keeps its node identity by pointing at the state directory the manual
# `podman run` deployment used (scripts/migrate-legacy.sh derives this).
TS_STATE_DIR=%h/podman/woow-tailscale/state
